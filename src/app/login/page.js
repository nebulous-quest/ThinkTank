import LoginForm from '@/components/LoginForm';

export const metadata = {
  title: 'Login - ThinkTank',
  description: 'Sign in to your ThinkTank account to start sharing your thoughts.',
}

export default function Login() {
  return <LoginForm />;
}