import RegisterForm from '@/components/RegisterForm';

export const metadata = {
  title: 'Register - ThinkTank',
  description: 'Create your ThinkTank account to start sharing your thoughts with the world.',
}

export default function Register() {
  return <RegisterForm />;
}